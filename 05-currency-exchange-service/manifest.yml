applications:
- name: currency-exchange-service
  disk_quota: 400M
  instances: 1
  memory: 400M
  # https://github.com/cloudfoundry/java-buildpack/issues/663
  env:
    JAVA_OPTS: '-XX:ReservedCodeCacheSize=32M -XX:MaxDirectMemorySize=32M'
    JBP_CONFIG_OPEN_JDK_JRE: '[memory_calculator: {stack_threads: 30}]'
  buildpacks:
  - https://github.com/cloudfoundry/java-buildpack.git
  #u need to give path or jar and manifest should be  in the same folder if u want to use cf push using manifest
  path: target/currency-exchange-service.jar
  routes:
  - route: currency-exchange-service-raja9580.cfapps.io   
  stack: cflinuxfs3
