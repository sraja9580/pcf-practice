server.port=8081
#THIS APPLICATION NAME SHOULD SAME AS CONFIG FILE CREATED IN CLOUD CONFIG SERVER
spring.application.name=limits-api
spring.cloud.config.uri=http://localhost:8888

#Below one picks dev config from cloud config .this can be passed as arg during app starting also as boot is apps are jar files
spring.profiles.active=dev